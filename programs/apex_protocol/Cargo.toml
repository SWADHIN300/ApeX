[package]
name = "apex_protocol"
version = "0.1.0"
description = "ApeX perpetual futures DEX protocol"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "apex_protocol"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { version = "0.30.0", features = ["init-if-needed"] }
# Default features are required: Anchor's `#[derive(Accounts)]` macro references
# anchor_spl::token_2022 and token_interface whenever `token::mint` constraints
# are used, as initialize_market and initialize_spot_market both do.
anchor-spl = { version = "0.30.0", features = ["token"] }
